Steps to follow:

step-1: install node(If already installed please ignore)

--> https://nodejs.org/dist/v20.16.0/node-v20.16.0-x64.msi

click this link and download node then install it.


step-2: download.

-> download this folder and save in a directory
-> open this folder in a code editor (VS code) and open terminal.
-> run following commands:
	>> npm install



step-3: run server

-> open terminal and run this command:
	>> node server.js